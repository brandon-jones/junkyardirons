.row
  .col-md-10.col-md-offset-1.main-container
    %h1.text-center Edit Instagram Tags
    Type in the tags you want to scrape off instagram. Seperate the tags with commas. Leave empty to get image with no tags.
    %br
    %br
    = form_tag("#{instagram_update_tags_path}", html: {class: "form"}) do
      .form-group
        %input{:name => "tags", :type => "text", class: 'form-control', :value => @instagram_tags, :id => 'instagram-tags'}
      .form-group
        %input{:type => "submit", :value => "Update Instagram Tags", class: 'btn'}
    %br
    %h3.text-center Current Instagram Tags
    - @instagram_possible_tags.each_slice(3) do |tags|
      .row
        .col-md-4
          = render partial: 'tag_helper', locals: { tag: tags[0], tag_array: @instagram_tags_array }
        .col-md-4
          = render partial: 'tag_helper', locals: { tag: tags[1], tag_array: @instagram_tags_array }
        .col-md-4
          = render partial: 'tag_helper', locals: { tag: tags[2], tag_array: @instagram_tags_array }